# Project: Sample Project

> Copy this file into every new project folder and fill in the details below.
> Claude Code reads this automatically when you open the SDK root.

## What this product is

<!-- Describe the product in 1-2 sentences. What does it do? Who uses it? -->
Example: A SaaS dashboard for operations teams to monitor fleet activity in real time.

## Pages in this project

<!-- List the HTML files in this folder and what each one does. -->
| File | Purpose |
|---|---|
| `welcome.html` | Onboarding welcome screen shown to new users |

## Users

<!-- Who uses this product? What's their technical level? What are they trying to do? -->
Example: Operations managers, non-technical, checking dashboards daily on desktop.

## Conventions for this project

<!-- Any naming, layout, or component preferences specific to this project. Delete if none. -->
- Primary action color: use `--color-primary` (inherits from brand theme)
- All data tables use `<fresh-data-table>` with row selection enabled
- Navigation pattern: persistent sidebar

## What not to do

- Do not add new CSS files — page-specific styles go in a `<style>` block in the HTML file
- Do not modify anything inside `../../systems/` — that is shared across all projects
- Do not use raw HTML for anything in the FreshDS component inventory (see root `CLAUDE.md`)
